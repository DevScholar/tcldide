# pnpm's default isolated linker requires symlinks that don't survive on
# /mnt/c (Windows filesystem mounted in WSL). Hoisted flattens node_modules
# like npm/yarn so cross-OS installs work.
node-linker=hoisted
